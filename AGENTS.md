# Agent Instructions

## Code Changes

- 如果涉及代码改动，必须在代码改动前和代码改动后都做相应的 `commit`。
- 每次 `commit` 的 message 必须包含与改动匹配的关键解释。

## Answering Style

- 回答必须先给直接结论，再给关键原因。
- 用户没有追问时，不要展开大量背景、例子或备选方案。
- 不确定时必须明确说“不确定”，不要用“通常”“可能”替代判断。
- 回答涉及具体项目功能时，必须结合代码进行说明。
- 解释函数时，必须用输入和输出说明函数行为。
- 用户问“哪个文件”“哪些代码”“哪里定义”时，必须给具体文件路径和行号。
- 产品行为问题必须优先按用户真实操作语义判断，再解释现有代码实现。
- 不要把临时方案说成最终方案；必须说明边界和风险。

## Project Explanations

- 解释项目整体时，必须优先从以下角度中选择相关部分说明：
  - agent 设计
  - agent 架构 / 工作流
  - 工具设计
  - 工具调用方式
  - log 设计
  - API 设计
  - 前端设计与用户旅程
  - 测试方法，包括 CLI、API、前端
  - 部署方式
  - 数据存储与用户分区方法
- 总结当前 session 对话时，也应优先按上述角度组织内容。
- 不要为了套结构而硬展开；如果问题只涉及函数、接口或局部代码，只解释相关部分。

## Docker Deployment Pitfalls

- 不要把本地开发机环境默认套用到远端部署环境。
- 讨论 Docker 部署问题时，必须先区分开发机、远端宿主机、正在运行的服务容器、临时管理容器。
- 不要默认远端宿主机有源码、Python3、虚拟环境或项目脚本。
- 如果服务是镜像部署，管理类命令应优先考虑通过镜像内脚本或临时管理容器执行。
- 不要把需要持久化的数据写进正在运行的服务容器内部临时文件系统。
- 需要被服务长期读取的数据，应写入服务容器共享的挂载目录。
- 多用户或多租户场景下，优先挂载总数据目录，再由程序内部按用户或租户分区；不要轻易为每个用户单独增加 compose 挂载项。
- 解释 Docker 挂载方案时，必须说清楚：输入文件从哪里来、命令在哪里执行、最终写到哪里、服务如何读取。
